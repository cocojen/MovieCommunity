from .serializers import MovieSerializer
from .models import Movie

def convert():
    print('converting --------------------------------------------')
    # mymovie = Movie.objects.filter(pk=2)
    # with open(r'/movie.json', 'w') as out:
    #     mv = MovieSerializer(mymovie)
    #     out.write(mv)

